== Welcome to cd verwaltung

To install, checkout the git repository:

git clone git@github.com:jsaedtler/cd_verwaltung.git


install bundler:

gem install bundler



install dependencies:

bundle install


setup database:

rake db:migrate



run:

rails server