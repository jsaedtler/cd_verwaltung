!!!
%html
  %head
    %title CdVerwaltung
    = stylesheet_link_tag 'blueprint/screen'
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    .container
      .span-24.last
        = render :partial => '/layouts/navigation'
        - if flash[:notice]
          .success 
            = flash[:notice]
        - if flash[:error] 
          .error 
            = flash[:error]
      .span-24.breadcrumbs.last
        = yield :breadcrumbs
      .span-4 
        = yield :sidebar
      .span-20.last
        = yield
      .span-24

