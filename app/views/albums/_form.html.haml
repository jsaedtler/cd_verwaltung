= form_for [@artist,@album], :html => { :multipart => true } do |form|
  = form.error_messages
  .field
    = form.label :title
    = form.text_field :title
  .field
    = form.file_field :cover
  .action
    = form.submit
